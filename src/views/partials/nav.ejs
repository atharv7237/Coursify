<nav class="border-b border-orange-500/20 bg-zinc-900/90 backdrop-blur-md sticky top-0 z-50">
    <div class="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center">
        <a href="<%= typeof currentUser !== 'undefined' && currentUser ? '/roadmap/home' : '/' %>" class="flex items-center space-x-2">
            <span class="text-2xl font-black tracking-tight text-orange-500 hover:text-orange-400 transition">
                Coursify
            </span>
        </a>

        <div class="flex items-center space-x-4 md:space-x-6 text-sm font-medium">
            <% if (typeof currentUser !== 'undefined' && currentUser) { %>
                <a href="/roadmap/home" class="text-zinc-300 hover:text-orange-500 transition">Create</a>
                <a href="/roadmap/dashboards" class="text-zinc-300 hover:text-orange-500 transition">Dashboard</a>
                <a href="/auth/logout" class="bg-zinc-800 hover:bg-zinc-700 text-zinc-300 hover:text-white px-3.5 py-1.5 rounded-lg border border-zinc-700 transition">
                    Logout
                </a>
            <% } else { %>
                <a href="/" class="text-zinc-300 hover:text-orange-500 transition">Sign In</a>
            <% } %>
        </div>
    </div>
</nav>
